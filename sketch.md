这个项目的立项完全就是为了实现一个不被反爬虫检测的 ai 可使用的 浏览器工具
痛点来源
我们二次封装 chrome-devtool-mcp 的 cdt 从 chrome-devtool-mcp 继承过来的自动化痕迹无法抹除，或者说我懒得去抹除了
所以我需要一个新的浏览器操作工具，使用python 的 DrissionPage
这是我对比出来的情况 playrigth 流行，但是每次都需要下载他的指定浏览器，很烦人，而且他的集成好像比较深入，所以反爬能够很好的检测到它，大部分时候都是需要一个特定的指纹浏览器，需要额外下载，也很烦人
DrissionPage 直接控制电脑当前的 默认浏览器(只支持 Chromium 内核的浏览器)-edge 或者 chrome 而且是有头，指纹就不用伪造了，而且仿照 cdt 的浏览器插件对新浏览器实例进行 cookie 注入也需要担心指纹，毕竟有些平台在指纹变化之后会风控账号，我们这样就没有账号风控的风险了
现在需要确定的是 DrissionPage 的多浏览器实例怎么样，能够满足 cdt 对 chrome-devtool-mcp 的二次封装的功能吗，以及是否能够完美复刻 chrome-devtool-mcp 的所有 Tools 特别是 take_snapshot 它能够穿透 shadow-root 这是很强的一个特性，以及 我自己想额外加上的一下功能，比如 在 take_snapshot 的返回结果中增加一个状态量-窗口的可滑动性，就是有些网页是还可以向下滑动的，而且有些内容是需要向下滑动才能展示的，而一般来说 ai 使用 take_snapshot 之后在找不到元素之后就没有下一步的辅助证据来帮助它推断下一步该怎么做，所以这里就仿照人类，我在当前视窗看不到我想要的元素，我就尝试左右滑或者上下滑动，而且一般人类在视觉上就能分析它是否能够左右滑或者上下滑动，而 ai 在使用工具时它并没有以视觉先行，我们也不希望它以视觉先行，毕竟现在的多模特模型还不算强，而且这样对 ai 来说效率很低，所以我就想引入这个 状态量。
